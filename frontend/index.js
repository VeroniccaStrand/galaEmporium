/* window.addEventListener("scroll", function () {
  if (window.scrollY > 70) {
    // Navbar shows up if scrolling down
    document.getElementById("navScroll").style.visibility = "visible";
  }
  if (window.scrollY < 70) {
    // If compeltely scrolled back up, navbar becomes hidden
    document.getElementById("navScroll").style.visibility = "hidden";
  }
});
 */
